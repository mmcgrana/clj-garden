(use 'clj-time.core)
(import '())

(def cookie-da )

(prn (.print formatter (now)))
;"%a, %d-%b-%Y %H:%M:%S GMT"
;
;(def #^{:private true
;        :doc "strftime formatter for Set-Cookie expiry header"}
;  +cookie-expiry-format+ "%a, %d-%b-%Y %H:%M:%S GMT")
;
;(def cookie-datetime-formatter
;  (DateTimeFormat/forPattern "")
;  ; DateTimeFormatter fmt = DateTimeFormat.forPattern("yyyyMMdd");
;  )
;(def cookie-datetime-str
;  [expires])
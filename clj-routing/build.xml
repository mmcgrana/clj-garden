<project name="clj-routing" default="jar">
  <description>Pack all sources into a JAR.</description>

  <property name="jarfile" location="clj-routing.jar"/>

  <target name="clean" description="Remove generated files and directories.">
    <delete file="${jarfile}"/>
  </target>

  <target name="jar" description="Create jar file.">
    <jar jarfile="${jarfile}">
      <path location="LICENSE"/>
      <fileset dir="./src" includes="clj_routing/**/*.clj"/>
    </jar>
  </target>
</project>
